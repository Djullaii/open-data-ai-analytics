apiVersion: v1
kind: Service
metadata:
  name: open-data-web
  namespace: open-data
  labels:
    app: open-data-web
spec:
  type: NodePort
  selector:
    app: open-data-web
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30080
